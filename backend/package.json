{
  "scripts": {
    "dev": "DB=test-db.json npx nodemon src/index.ts --ignore 'test-db.json'",
    "build": "npx tsc",
    "prettier": "prettier --write ."
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "typescript": "^4.8.4"
  }
}
